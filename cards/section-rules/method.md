# 卡片 · 方法章

**触发**：`noun_chain` / `triple_de` 密集落在方法章（style_audit）；`jargon_hit`（内部标识进正文）；人工判定「小节首句直接给符号或公式」「实现细节当方法」「有名无值的限定语」。依据：02-narrative §方法章、§动机句、§实现细节移出正文但数值参数留下；03-diction §十五；07-code-consistency。

## 动笔前（写作契约）
写这一章之前读这一段；写完跑 `run_gates.py --stage chapter`（16-drafting-loop §三）。
- 每小节节拍：动机句 → 对策 → 公式 →（必要时）算法框；动机句写成「由于 X 无法直接获得，需要…」。
- 排序键、去重键、配置键名、变量名不进正文；容差、上限、步长、门限工作点留在正文并就地给数值。
- 限定语有名有值：「预设上限」要么补数值，要么整句删。
- 符号在首次出现处定义；伪代码体按期刊惯例写英文（10-chinese §六）。

## 允许改
- 每小节补动机句：「由于 X 无法直接获得，需要 ……」，放在符号定义与公式之前。
- 把排序键、去重键、配置键名、变量名从正文移出（移进算法框或删除），正文只留机制。
- 「预设上限 / 给定门限」这类空壳限定语：补上已在正文或宏里存在的数值，或整句删。
- 名词链拆解（走 `noun-chain-unpack`）。
- 复杂度段按 07-code-consistency 核对生产调用链后改措辞。

## 禁止改
- 公式、符号定义、算法框的逻辑（要改先改代码或大纲，再回来）。
- 任何数值参数的值——只允许「补已有值」，不允许「换值」。
- 已冻结术语的字面。
- 小节顺序（结构层）。

## 必保留
- 每小节节拍：动机句 → 对策 → 公式 →（必要时）算法框。
- 容差、上限、步长、门限工作点的数值留在正文。
- 02-narrative §压缩方法章的不可删区域列出的内容。

## 自检与停止
- 逐小节只看首句：没有一节直接以符号定义或公式开头 → 停。
- `jargon_scan`（排除 `algorithmic`）零残留；`semantic_diff` 方法章数值集合不变。
- 同一小节最多 2 次。
