# 卡片 · 讨论（含结果章末的讨论段）

**触发**：`hedge_duplicate` / `hedge_over_budget` 集中在讨论（hedge_budget）；`generic_closing`、`claim_far_from_evidence`（style_audit）；人工判定「讨论重复结果章」「局限反复致歉」。依据：02-narrative §讨论是否独立成章；04-hedging §二、§十一；SKILL §五 成对规则。

## 动笔前（写作契约）
写这一章之前读这一段；写完跑 `run_gates.py --stage chapter`（16-drafting-loop §三）。
- 先判断是否独立成章：结果章每图已配「现象 + 机制」时，讨论只做跨图综合与边界。
- 每条局限全稿只说一次；正面结论在前，失效条件在段尾。
- 不引入正文没铺垫过的新结论、新归因、新外推。

## 先判断
- 讨论是否独立成章：结果章每图已配「现象 + 机制」时，讨论只保留跨图的综合与边界，不复述单图现象。
- 每条局限在全稿是不是只在此处出现一次？已在结果章说过 → 这里只呼应一句，不重述。

## 允许改
- 合并同一边界的重复陈述（保留主张处 1 次 + 结论呼应 1 次）。
- 把「不能证明 A、不表示 B、也不应解释为 C」压成一句真实边界。
- 删泛化收束句；用具体的未解决问题替代「未来工作将进一步 ……」。
- 正面结论排在前，失效条件移到段尾（04-hedging §十一）。

## 禁止改
- 两问都答「是」的唯一边界（04-hedging §三）。
- 带豁免标签的句子；「消融结果尚不能区分各部件独立贡献」类的归因边界。
- 引入正文没有实验支持的新归因或新外推。
- 与结论章重复的措辞——改一处必须同步另一处（走 `hedging-balance`）。

## 必保留
- ≥ 1 句适用范围（`hedge_budget` 下限计入 results 或 conclusion 角色；讨论若独立成章，配置 `roles` 里把它映射到 results）。
- 做到的仍然写在最前（成对规则：不因归因受限就用失败开头）。

## 自检与停止
- `hedge_budget` 转绿（上限、同边界 ≤ 2、下限三项）。
- 复述：讨论每段都能指名它综合的是哪几张图 / 哪条闭环表主张。
- 同一段最多 2 次。
